# AIRP Version History

Per-version scope for the 0.7.x line and the 0.8.0 release.

This table previously lived only in `GEMINI.md`, which was gitignored. It was the
most detailed record of what each version changed and existed nowhere else in the
repository. It is tracked here so it survives a fresh clone.

`WS` is the workstream label used during the 0.8 refactor planning.

| Version | WS | Scope | Status |
|---|---|---|---|
| 0.7.18 | F | Clickable markdown links (`url_launcher` + `onTapLink`); remove deprecated long-press 4-icon menu; bubbles unfrozen; keep `MessageBubbleActions` (regen/copy/edit/delete/branch/version picker) | done |
| 0.7.19 | E | Inline API & Model panels (drop `ExpansionTile` wrappers); BYOK copy/paste; Load/Refresh Models button next to BYOK; removed header reload button | done |
| 0.7.19.1 | E | Replace header texts with `Divider`; dynamic Load-vs-Refresh label (empty list to Load, else Refresh); drop per-provider `Refresh Model List` button; spacing 4px | done |
| 0.7.19.2 | E | Remove duplicate divider (SettingsHeader already has one); spacing 4 to 2; local/ollama refresh fallback (endpoint-adjacent Load/Refresh button); sync `version.dart` | done |
| 0.7.20 | C | Remove reasoning storage toggles (efficiency/persist/developer/raw-edit/restore-backup); reasoning ALWAYS displayed via per-bubble "Show Thought Process", ALWAYS stripped from OUTBOUND payload, persisted in session JSON. Removed `_shouldStripReasoningFromStorage`, `_normalizeReasoningStorageMode`, `_applyReasoningStoragePolicyGlobally`, `SessionService.applyReasoningStoragePolicyGlobally`, backup keys, `ChatMessage.sanitizeForStorage`. `editMessage` always re-prepends reasoning; `getEditableMessageText`/`getReadOnlyReasoningForEdit` always split. Keeps `enableReasoning` + effort dropdown (outbound request control). Fixes both-enabled bug + completion-truncation bug. | done |
| 0.7.21 | D | Drop `WebSearchMode` (Smart/Eager) enum + dropdown + `prefKeyWebSearchMode`; single unified always-on hint (`buildWebSearchSystemHint({maxRounds, resultCount})`) with both max searches + max results injected + a light RP guardrail. Fix thinking-blocks-disappear-when-web-search-on: non-streamed direct answers now wrap `det.reasoning` in think tags via `_composeDirectAnswer` + carry `reasoningRecovered` through `_WebSearchLoopResult` and the fast-path `copyWith`. | done |
| 0.7.22 | A | Unified `ConfigPack` (`lib/models/config_pack.dart` + `lib/services/config_pack_service.dart`) capturing ALL settings-drawer state except conversations + API keys (chat+settings+theme+vfx+scale export maps, `sessions` stripped); in-app named packs (`airp_config_packs` SharedPreferences, newest-first) with tap-to-apply (overwrite, confirm) / rename / delete / export-to-file / import-from-file; tolerant SillyTavern Chat-Completion preset IMPORT-only (`importSillyTavernPreset`: ordered `prompts`+`prompt_order` to systemInstruction, generation fields to generation bucket, `sourceFormat='sillytavern'`). Removed per-rule CRUD + `airp_custom_rules` store, `SystemPreset` model + `preset_model.dart`, `LibraryService.exportPreset`/`_importCustomRules`. Removed `SizedBox(height:2000)` pit (now responsive `MediaQuery.height*0.6` + per-tab `SingleChildScrollView`). | done |
| 0.7.23 | B | Snapshots tab to embedded conversation list; each entry shows created-timestamp subtitle (parsed from session `id` epoch-millis) + message count, with per-conversation export (`.airp`) + delete, plus "Import .airp" (merges via `LibraryService.importLibrary`) and "Export All" (conversations-only `.airp`); dropped the full-state category toggles (ConfigPack handles settings now). Fixed `sessions` preview-path bug (`data['settings']['sessions']` to top-level `data['sessions']`). | done |
| 0.7.24 | G | Search drawer: magnifying-glass icon on the leftmost header (next to menu) + Ctrl/Cmd+F; `SearchProvider` computes matches (one `SearchMatch` per occurrence across `chatProvider.messages`); find-bar overlay (`ChatSearchBar`) slides down from under the AppBar with "x / y" count + prev/next + Esc to close; matches highlighted on `MessageBubble` via `isSearchCurrent`/`isSearchMatch` (border + bloom glow); current match auto-scrolled into view via `Scrollable.ensureVisible` (per-message `GlobalKey` map in `ChatMessagesListState.scrollToMessage`); Ctrl/Cmd+G = next, Shift/Cmd-alt variants = prev; query persists while open, clears on close; `ChatMessagesList` converted StatelessWidget to StatefulWidget; `themeProvider.bloomGlowColor` reused as the highlight accent. | done |
| 0.7.25 | H | Summarize drawer: `Icons.compress` tab on the leftmost header (3rd icon); `SummarizeDrawer` slides from left with two editable monospace prompt fields (compress + voice samples, defaults `SummarizeDefaults.defaultCompressPrompt`/`defaultVoiceSamplesPrompt`, persisted via `airp_summarize_compress_prompt`/`airp_summarize_voice_prompt`/`airp_summarize_pair_count` SharedPreferences) and a 1-20 slider (default 5, counts assistant-turn pairs anchored on the assistant side); **Compress** issues two sequential one-shot LLM calls via `ChatProvider._oneShotGenerate` (Gemini uses `performGeminiFunctionDetection` with empty functionDecls; OpenAI-compatible uses `requestOpenAiCompatibleWithToolDetection` with empty tools; uses active strategy/key/url/model; generation settings passed through) producing a narrative summary + voice samples; appends both as `**[Summary]**`/`**[Voice Samples]**` AI messages to the current chat; creates a fresh `ChatSessionData` = [summary]+[voices]+[last N pairs] (walks backwards anchoring on assistant turns, copies preceding user turns) via `_sessionService.prependSession`; auto-switches to it; original conversation left intact (only appended-to). `SummarizeDrawer` shows inline status; closes on success. | done |
| 0.7.26 | I | Header layout, title bug, word highlight, context counter, timestamps | done |
| 0.7.27 | J | **Fix Gemini/Gemma streaming (`alt=sse`).** `streamGenerateContent` was requested without `alt=sse`, so it returned a pretty-printed JSON array instead of Server-Sent Events; no line carried the `data: ` prefix the parser looks for, every line was skipped, and the stream completed empty behind an HTTP 200. Gemini and Gemma streaming had been silently dead since `0.7.17` (nine versions). URL construction extracted to `ChatApiService.buildGeminiStreamUrl` and guarded by `test/gemini_stream_url_test.dart`. Added an empty-stream notice (`emptyGeminiStreamNotice`) so a 200-with-no-content reports `blockReason`/`finishReason` instead of rendering a blank bubble; reasoning-only responses are left to the existing `StreamingCoordinatorService` recovery path. Also lands the lorebook retirement: purge the machinery orphaned by `0.6.11.1`. Deleted `lorebook_state_service.dart`, `PromptPipelineService.evaluateLorebooks`, positional injection in `buildSystemInstruction`, `LorebookEvalResult` + activation traces, timed effects, and the always-empty diagnostics panel. `LorebookService.evaluateEntries` became `matchEntries` returning a flat order-sorted list. SillyTavern V2 spec fields retained for import/export fidelity. Also removed `ExportOptions` + `LibraryService.exportLibraryAsync` (superseded by ConfigPack in 0.7.22), `FileIOHelper.saveString`/`writeBytes`, `ChatProvider.isCancelled`, `prefEnableImageGen`, and the stale regex/formatting comment. Docs restructured under `docs/`; `CLAUDE.md`, `AGENTS.md`, `GEMINI.md`, `LICENSE` now tracked. | done |
| 0.7.28 | K | **Atmospheric effects rewrite + CRT background.** The old overlay built one `LoopAnimationBuilder` and one full-screen `CustomPaint` *per particle* (fireflies nested a second controller inside the first), so defaults meant ~80 animation controllers and ~80 compositor layers per frame, each doing a `MaskFilter.blur` gaussian. Replaced with one `Ticker` and one `CustomPaint` for all effects; glowing particles render in a single `Canvas.drawAtlas` call against a sprite sheet rendered once at startup. The ticker stops entirely when no effect is enabled. Motion is now closed-form in `t` (resolution independent, resumable, unit-testable). **Motes:** depth-based parallax, two-term sine wander instead of a straight A-to-B lerp, twinkle. **Rain:** one shared wind slant for the field (was derived per drop from its own length, so drops crossed at different angles), streak length proportional to fall speed, depth layering, visible opacity. **Fireflies:** no longer a recoloured mote. Four-term incommensurate-sine flight, discrete blink envelope (fast rise, brief hold, decay, then a long dark interval that is the majority of the cycle), small bright core inside a wide halo, bioluminescent yellow-green hue band. **New `CRT Screen` effect:** `shaders/crt.frag` fragment shader applying barrel/fisheye distortion, chromatic aberration, rolling scanlines, aperture grille and vignette to the background image only, with a `CRT Intensity` slider. Text is deliberately excluded so glyphs stay flat and crisp. Falls back to a plain `BoxFit.cover` image if the platform cannot compile the shader. | done |
| 0.7.28.1 | K | **CRT reworked around fuzz; effect sliders are real counts.** 0.7.28 shipped the CRT as a background-only barrel warp, so raising intensity just deepened a fisheye wallpaper while text stayed untouched. Rewritten: `CrtScreen` wraps the whole chat surface in a `SnapshotWidget`, rasterises it, and runs background, particles and text through `shaders/crt.frag` together. The shader now leads with analog artifacts (seven-tap horizontal smear for lost bandwidth, bloom bleed from bright areas, colour separation growing toward the edges, rolling scanlines, aperture grille, per-frame grain, stronger vignette); barrel distortion is capped at `0.045` so curvature is felt, not seen. Replaces `CrtBackground`. **Slider fix:** each effect now draws a prefix of a fixed, fixed-seed pool instead of regenerating the whole field on every change, which had made the slider behave like a reseed. Sliders are percentages of `kMaxMotes` / `kMaxRainDrops` / `kMaxFireflies`, so 50% always draws the same first half and raising the value only adds particles. Stored raw counts migrate once to the percentage scale via `VfxProvider.percentFromLegacy`. **Motes:** cubic-weighted radius distribution spanning minute specks to markedly larger out-of-focus ones, with big specks held dimmer; twinkle unchanged. | done |
| 0.7.28.2 | K | **Fix frozen chat under CRT; split scanline and fisheye controls.** 0.7.28.1 used `SnapshotWidget`, which caches its raster: its painter's `notifyListeners` repaints "re-using the same raster" by design, and children behind a `RepaintBoundary` (the message list and effects overlay both are) never mark it dirty. The chat therefore froze at the first frame while scanlines kept rolling. Replaced with a `RenderProxyBox` (`_RenderCrtEffect`) that paints the child into a detached `OffsetLayer` and `toImageSync`es it **every** frame, mirroring the framework's own `_paintAndDetachToImage`. Content is live again; the cost is a full-surface rasterise per frame, inherent to a live post-process. Falls back to painting the child untreated when a subtree cannot be rasterised. **New controls:** `CRT Scanlines` slider drives scanline and aperture-grille visibility from its own uniform, independent of master intensity, so at 0 the raster structure disappears and only the signal artifacts (smear, bloom, chromatic aberration, grain) remain. `CRT Fisheye` switch gates the barrel warp entirely; off by default, and when off the sampling grid is untouched at any intensity. Both persist and round-trip through config packs. | done |
| 0.7.28.3 | H | **Fix inverted pairs in the summarize branch.** The trailing-pair walk in `compressAndBranch` inserted the user turn at index 0 and then inserted its assistant turn at index 0 as well, pushing the reply above the message that prompted it. A three-turn chat compressed with 2 pairs produced `[Summary][Voices] AI2 User2 AI3 User3`, so the branched conversation read answers-before-questions on screen and in the outbound context. Assistant is now inserted first. Walk extracted to `ChatProvider.collectTrailingPairs` and guarded by `test/chat_provider_compress_pairs_test.dart`; the drawer had no test coverage at all. | done |
| 0.7.29 | L | **Background completions reach disk; web search streams again.** Two unrelated defects that both made a conversation read as shifted by one turn or feel dead. **Persistence:** `SessionService.finalizeBackgroundSession` and `addMessageToSavedSession` mutated `savedSessions` and returned without calling `persistSessions()` or `onStateChanged()` — every other mutator in the file does. A reply that landed while its conversation was not the active one existed only in memory, so on restart the prompt was followed by the empty assistant message that seeds a stream (`AUDIT-0.8-ADDENDUM.md` 2.2). That blank bubble renders as a gap, putting every later reply one slot below the message that prompted it. Both now persist. Added `SessionService.dropEmptyAssistantPlaceholders`, applied in `loadSessions`, which repairs conversations already damaged on disk; it keeps any assistant message carrying text, images, usage or regeneration history. Autosave is a 600 ms debounce and nothing flushed it on the way out, so a close inside that window dropped the last turn: `AIRP` is now a `StatefulWidget` with a `WidgetsBindingObserver` calling `ChatProvider.flushPendingSave` on paused/hidden/detached, backed by `SessionService.flushPendingSave`. The stream-error path no longer appends a second message after the placeholder, which stranded a blank turn between the prompt and the error. **Web search:** the BYOK tool loop opened with a NON-streaming detection round, so whenever web search was on and the model answered without searching — the common case — the whole response including reasoning arrived as one blob. Replaced with `ChatApiService.streamOpenAiCompatibleWithToolDetection`, which attaches the tool to a streamed request and classifies from the first deltas: tool call fragments mean search, content or reasoning means this is the answer and the live socket is handed straight to `StreamingCoordinatorService`. One request, no discarded generation, reasoning streams. Gemini keeps its non-streamed detection. Reasoning was also being lost outright: the detection call never set `include_reasoning`, which OpenRouter requires, and `performGeminiFunctionDetection` never checked `thought: true`, concatenating Gemini's thinking into the visible answer as prose — now split via `splitGeminiThoughtParts`. That Gemini request also carried no `generationConfig` at all, silently discarding temperature/topP/topK/maxOutputTokens on every turn with web search on. The search-progress bubble now shows queries already run plus the in-flight one with a round counter instead of overwriting a single line. History limiting was applied only on the plain send path, so enabling web search sent the entire conversation; both paths now share `_limitedHistory`. Adds `async` as a direct dependency for `StreamQueue`. | done |
| 0.7.29.1 | M | **Token counting anchored to reported usage; usage badge fixed.** The context meter shipped in `0.7.26` measured something the app never sent. Its estimate branch summed the raw `_systemInstruction` field, so the character card, persona, recognized lore, depth prompts and the web-search hint — everything `PromptPipelineService.buildSystemInstruction` assembles — were invisible to it, while both branches walked the entire conversation and ignored `historyLimit`, over-counting in the other direction. Meanwhile the exact `prompt_tokens` every provider reports arrived each turn, was rendered in a badge, and was then discarded. New `lib/utils/token_utils.dart`: the meter is now anchored to the last reported `prompt_tokens` and estimates only what was added since, scaled by a per-provider calibration ratio (`airp_token_calibration`, EMA-smoothed, clamped to 0.5–2.0) that converges on the provider's own accounting after one exchange. The anchor delta may be negative, so deleting or shortening a message inside the anchored window shrinks the meter. `loadSession` re-anchors from the most recent stored usage reading. The Gemini branch's `_model.countTokens` round trip is gone: it cost a request per turn, needed a valid key, and left a silently stale number on `debugPrint` whenever it failed. The estimator drops the `+0.3 tokens/word` term, which double-counted on top of an already space-inclusive chars/4 rate, and images are charged at Gemini's 258/tile rather than a flat 200. **Usage badge:** `chat_api_service` forwards Gemini's camelCase `usageMetadata` verbatim but the widget read only OpenAI snake_case keys, so every Gemini turn displayed `0 in + 0 out = 0 total`. `TokenUtils.normalizeUsage` maps Gemini, `input_tokens`/`output_tokens` gateways and nested `*_details` objects onto one shape, surfacing reasoning and cached counts; it also runs on read, so sessions saved by older builds render retroactively. Gemini's `candidatesTokenCount` excludes thoughts, which are added back so `completion_tokens` includes reasoning as OpenAI defines it. The meter now refreshes on delete, edit, session load and provider switch instead of only at end of stream, and the `enableGrounding` guard that froze it permanently once Gemini grounding was on is removed. `LorebookService` delegates to the shared estimator, replacing a 1.3-tokens-per-word heuristic that ran about a quarter low and let budgeted entries overshoot. App bar thousands-separates the current count as it already did the maximum. Guarded by `test/token_utils_test.dart` and `test/chat_provider_token_count_test.dart`; the counter had no coverage at all. | done |
| 0.7.29.2 | M | **Font catalogue expanded; model info reads instead of truncating.** The font list was two hand-maintained lists that had to agree: a 15-case switch in `ThemeProvider.currentTextTheme` and a hardcoded 16-item `DropdownMenuItem` list in `text_designer_panel`. Collapsed into `lib/utils/app_fonts.dart`, one ordered table of `AppFont(key, label, category, builder)` that both read from; the menu is generated and grouped by category. Every key that ever shipped is preserved verbatim — they are persisted under `app_font_style` and travel in config packs, so renaming one silently resets a user's font — and `AppFonts.resolve` falls back to the system default for a key written by a newer build. 25 faces added across Interface (Nunito, Montserrat, Poppins, Atkinson Hyperlegible, Lexend), Serif (Merriweather, Libre Baskerville, Cormorant Garamond, Spectral, Bitter), Monospace (JetBrains Mono, IBM Plex Mono, Fira Code), Handwriting (Dancing Script, Indie Flower, Patrick Hand, Shadows Into Light), Display (Cinzel Decorative, MedievalSharp, Uncial Antiqua, Pirata One, Comic Neue) and Japanese (Noto Sans JP, M PLUS Rounded 1c, Sawarabi Mincho). `google_fonts` fetches faces on first use rather than bundling them, so this costs nothing in app size but a newly picked face needs one network fetch before it renders. **Model info:** the "Selected Model Details" description was capped at `maxLines: 3` with an ellipsis and no way to read the rest, which hurt most on OpenRouter where descriptions run to several paragraphs. It is now a bounded eight-line box that scrolls, with an always-visible scrollbar; the details dialog in the model picker gets an explicit half-screen height and its own scrollbar. Both take an explicit `ScrollController` rather than claiming the ambient `PrimaryScrollController` the settings drawer already owns. **New `lib/utils/model_details.dart`:** `ModelInfo.rawData` held the full provider payload and was rendered nowhere. `ModelDetails.extract` pulls the metadata worth showing — owner, canonical slug, modality, input/output modalities, tokenizer, instruct type, provider context and max output, moderation status, per-request limits, supported parameters — reading known keys wherever they appear so a provider that reports little simply yields a shorter list, and preferring OpenRouter's `top_provider` block over duplicate top-level keys. Guarded by `test/app_fonts_test.dart` and `test/model_details_test.dart`. | done |
| 0.7.30 | N | **Eleven providers retired; NVIDIA and Ollama repaired.** `AiProvider` went from twenty entries to nine (Gemini, OpenRouter, NanoGPT, NVIDIA, xAI, DeepSeek, Ollama, OpenAI Compatible, Local). ArliAI, Blackbox AI, Groq, HuggingFace, Minimax, Mistral, OpenAI, Qwen, Vertex AI, Xiaomi MiMo and Z.ai are gone, along with their strategies, forty-four `ApiConstants` entries, and their rows in every one of the eleven duplication sites `ARCHITECTURE.md` lists. `ThinkingFormat.enableThinking` (Qwen) and `ThinkingFormat.thinkingObject` (Z.ai) had no remaining implementor and were removed from the enum and from all three `chat_api_service` switches. A session or config pack naming a retired provider now degrades to Gemini instead of throwing, via the new `ChatProvider._providerFromName`, which also replaces the twenty-branch string chain in `_loadSettings` and the twenty-branch chain in `loadSession`; the old `loadSession` chain set `_selectedModel` but not the per-provider field for OpenAI and Groq, so switching away and back lost the model. **NVIDIA:** entering an API key issued no request at all. The model list stayed empty until the user found the separate refresh button, which reads as the key having been ignored, and any fetch that did run had its error swallowed by a `debugPrint` inside `ModelRegistryService`. `ChatProvider.setApiKey` and both endpoint setters now schedule a debounced fetch (900 ms, so a pasted key fires one request), the registry records a per-provider `lastError`, and the API panel shows the outcome under the button: model count on success, an actionable line on failure (`401/403` reads as a key problem, `404` as an endpoint problem, a socket error as unreachable). New `NvidiaStrategy` drops catalogue entries with no `/chat/completions` route (embedding, reranking, `nvclip`, retriever-parse), which otherwise 404 in a way that looks like a bad key, and sorts the remainder. **Ollama:** the endpoint field was never persisted and never used. `airp_ollama_endpoint` and `airp_openai_compatible_endpoint` were declared in `ApiConstants` but read and written nowhere, so a configured server reverted to `localhost:11434` on restart, and `customUrl` was only computed for Local, OpenAI Compatible and Vertex AI, so Ollama always streamed against the hardcoded default regardless. Both endpoints now load, save, export, and travel through `LibraryService` and config packs. New `OllamaStrategy` normalizes whatever root the user pastes (bare, `/v1`, or a full completions URL), chats over `/v1/chat/completions` and lists models over the native `/api/tags`, which unlike `/v1/models` reports family, parameter size, quantization and on-disk size. Model-list URL construction moved out of `ModelRegistryService` into `AiProviderStrategy.getModelsUrl`, so Local and OpenAI Compatible fetch their lists from the user's endpoint too instead of returning early on an empty base URL. The three copies of the endpoint if-chain in `ChatProvider` collapsed into `_customEndpointFor`. `ModelSettingsPanel` gained the model pickers it never had for DeepSeek, xAI, Ollama and OpenAI Compatible, and `_getModelCount` reads `currentModelsList` rather than a switch that returned 0 for half the providers. Guarded by `test/provider_strategy_endpoints_test.dart` and four new cases in `test/global_settings_persistence_test.dart`. | done |
| 0.7.30.1 | N | **xAI retired; every remaining provider's thinking mechanism verified against its own docs.** Eight providers remain. The reasoning work started from a wrong assumption in `0.7.30`, that `reasoning_effort` is the universal control and that omitting it means off. Checked against vendor documentation, three of the eight disagree. **Gemini does not use `reasoning_effort` at all** and never did: reasoning is requested through `generationConfig.thinkingConfig`, and thought parts are only returned when `includeThoughts` is true. AIRP sent neither, so the effort dropdown had no effect on any Gemini model. New `ChatApiService.buildGeminiThinkingConfig` emits `thinkingLevel` (`minimal|low|medium|high`) for Gemini 3 and newer and `thinkingBudget` in tokens for Gemini 2.x, reading the family off the model id because sending the wrong field degrades or rejects the request; budget `0` disables, and 2.5 Pro, which cannot disable thinking, falls back to `-1` (dynamic) instead of erroring. A non-`gemini-<n>` id gets `includeThoughts` alone. Wired into both `streamGeminiResponse` and `performGeminiFunctionDetection`, and only sent when the user has reasoning enabled, so each model keeps its own default otherwise. **Ollama** maps `reasoning_effort` onto its native `think` and auto-enables thinking for any thinking-capable model when the field is absent; omitting it on Disabled therefore meant default-on, so the field is now sent on every request. **DeepSeek** V4 defaults thinking ON at `high` and takes `thinking: {type: enabled|disabled}` with a `low|high|max` ladder that has no `medium`; AIRP was sending nothing, so thinking could be neither disabled nor tuned. New `DeepseekStrategy` sends the object and overrides `reasoningEffortOptions` to the four levels the API accepts. **NanoGPT** documents `reasoning_effort` as both depth control and mode signal across `none` through `xhigh`, so it is sent always and `Max` is offered. **NVIDIA** validates the field against `low|medium|high` and 400s on anything else, which is already what AIRP sends; the Nemotron `detailed thinking on|off` system-prompt mechanism is deliberately not implemented because it would overwrite the user's own instruction. `ThinkingFormat` gains `reasoningEffortAlways` and regains `thinkingObject`; the switch that applies it now lives once in `AiProviderStrategy.applyThinkingFormat` instead of once there and three more times in `chat_api_service`. `GenerationSettingsPanel._effectiveReasoningEffort` steps an unsupported stored level down to the nearest accepted one rather than reading as Disabled, which switching to DeepSeek from a `medium` setting would otherwise have done. Guarded by eleven new cases in `test/provider_strategy_endpoints_test.dart`. | done |
| 0.7.30.2 | N | **The Gemini SDK path was dead; removed it and rewrote the README against the code.** `ChatProvider.initializeModel` built a `GenerativeModel` and a `ChatSession` from the whole conversation, reading every attached image off disk to do it, and ran on session load, provider switch, message edit, delete, regenerate and every send. Nothing read the result. `GeminiStrategy.streamResponse` took the session as `providerSession` and ignored it, streaming over raw REST from `history` and `systemInstruction` instead. The method also appended `[SYSTEM: You are a reasoning model. Enclose your entire internal thought process in <think> and </think> tags]` plus `Reasoning Effort: <level>.` as prose, a prompt hack that never reached a request because the send path builds its own instruction from `PromptPipelineService`. Deleted the method, its fifteen call sites, the `_model` and `_chat` fields, the `providerSession` parameter on the strategy interface, and the `google_generative_ai` dependency, which had no remaining user after `0.7.29.1` removed the `countTokens` round trip. Every send now skips a full history rebuild and the disk reads that came with it. **README rewritten against the code rather than memory.** It still advertised nineteen providers, `Reasoning Efficiency Controls` and `Developer Safety Controls` (removed in `0.7.20`), per-rule Advanced Tweaks (removed in `0.7.22`), the Smart/Eager web search mode selector (removed in `0.7.21`), `Refresh Model List` (renamed in `0.7.19.1`), 16 fonts (42 since `0.7.29.2`) and raw particle-count sliders (percentages since `0.7.28.1`); none of those identifiers exist in `lib/` any more. Added the Summarize drawer (`0.7.25`), in-chat find (`0.7.24`), the CRT post-process (`0.7.28`), the anchored context meter (`0.7.29.1`), model-detail extraction (`0.7.29.2`), and a per-provider reasoning-format table replacing the claim that everything takes `reasoning_effort`. | done |
| 0.7.30.3 | N | **Markdown colour pickers cut from thirteen to three.** Roleplay prose is narration plus `"dialogue"` plus `*actions*`, with bold for emphasis; the SillyTavern convention the app is built around uses nothing else. The panel nevertheless offered thirteen pickers, and three of them (Bold Italic, Inline Code, Code Block) were never wired to the renderer at all: `MarkdownStyleSheet` has no bold-italic slot and `codeStyle` was built from `textColor`, so those three persisted a value nothing could read. Kept `paragraph`, `italic` and `bold`, relabelled Narration / Italic (actions) / Bold (emphasis). The other ten still render: `markdownStructureColor` (headings, list bullets, strikethrough) tracks `textColor`, blockquote tracks `subtitleColor`, and links keep a distinct hue because web search answers are full of them. Headings and lists were deliberately NOT dropped from rendering, because the Summarize drawer's default voice-samples prompt asks the model for "Markdown headings per character, with a bulleted list of samples below each heading", so first-party output depends on them painting. `updateMarkdownColor` now ignores an unknown type instead of falling through, so a config pack written while the retired pickers existed still applies; the orphaned `color_md_*` prefs keys are left on disk rather than deleted. Guarded by `test/theme_markdown_colors_test.dart`, which had no coverage before. | done |
| 0.7.30.4 | N | **Two text fields that fought the caret, and Local's unusable model entry.** The system prompt textarea trimmed its value inside `onChanged` and fed the trimmed string back through `ChatProvider`, whose `notifyListeners` re-entered `_syncControllers`, which saw a mismatch and assigned `controller.text`. Assigning that property collapses the selection to the end of the new value, so the loop ate a space typed at the end of the prompt and threw the caret to the end when one was typed at the front; a space typed anywhere in the middle survived, because trimming did not change the string and no resync fired. The trim is gone (nothing downstream wanted it: `PromptPipelineService` concatenates the instruction as given) and the sync now skips whichever field holds focus, so an external change still lands but never mid-edit. The same shape was in `ApiSettingsPanel` for the API key and all three endpoint fields; both trims moved to the read side, where `ApiKeyService.getProviderKey` and `ChatProvider._customEndpointFor` already were. **Local:** the Target Model ID field built a `TextEditingController` inline in `build()`, so every keystroke discarded the field's state and rebuilt it, and then called the full `saveSettings` -- roughly twenty awaited `SharedPreferences` writes, a lorebook JSON encode and a session autosave -- per letter. That is the second-long stall per character; no GET was involved. The controller is now built once in `initState` and the write goes through a new debounced `ChatProvider.saveSettingsDebounced` (600 ms), used by the conversation title and the API panel as well. Local was also the one provider with no model list at all, despite already resolving to `OpenAiCompatibleStrategy`, which lists over `/v1/models` -- what llama.cpp and LM Studio both serve. It now has `localModelsList` and `isLoadingLocalModels`, `setLocalIp` schedules the same debounced auto-fetch the other endpoint providers got in `0.7.30`, and `ModelSettingsPanel` renders the shared `ProviderModelSelector`: a picker once the server has been listed, the text field only as fallback. `ProviderModelSelector` had declared `isLoading`, `onRefresh` and `refreshButtonColor` since it was written but rendered none of them; they are now an inline refresh button for every provider. The app bar gets the same picker for Local. A blank Target Model ID now means Auto: new `ChatProvider.effectiveLocalModel` resolves it to the first discovered model, or `local-model` when nothing has been listed, because an empty `model` field is a 400 on some OpenAI-compatible servers. The default changed from `local-model` to blank, and `_getProviderModel` / `_setProviderModel` stopped special-casing Local -- it was the one enum arm that returned a display string and discarded writes. Guarded by `test/system_prompt_panel_caret_test.dart` and `test/local_provider_model_test.dart`. | done |
| 0.7.30.5 | N | **Switching provider left the previous provider's API key on screen, so OpenRouter sent an empty bearer token.** `0.7.30.4` gave `ApiSettingsPanel._syncControllers` a focus guard, because assigning `controller.text` mid-edit drags the caret to the end of the box. The guard was unconditional, and the API key box is one field standing in for eight separate credentials: switching provider while that field held focus skipped the resync, so the box kept showing the key of the provider the user had just left. It read as configured while the new provider's slot was empty, and `ChatApiService.streamOpenAiCompatible` sends `Authorization: Bearer $cleanKey` unconditionally, so the request went out as a bare `Bearer ` with nothing after it. OpenRouter answers that with `401 {"error":{"message":"Missing Authentication header","code":401}}`, verified against the live endpoint; its model list is public, so a successful Load Models gave no signal that the key was missing. A provider switch now overrides the focus guard and drops focus with it. Second, `ChatProvider.missingCredentialError` runs before the request and writes a named explanation into the reply bubble instead of letting an empty credential reach the wire, for a missing endpoint as well as a missing key. `AiProvider` gains a `displayName` and `needsApiKey` extension in `lib/models/chat_models.dart`, replacing the copy in `ChatAppBar` and the two open-coded provider comparisons. Guarded by `test/api_key_panel_provider_switch_test.dart`. | done |
| 0.7.30.6 | N | **Three more of the same family, found by auditing the rest of `0.7.30.4`.** (1) `flushPendingSave` drained the session autosave but not the settings debounce `0.7.30.4` introduced, so an endpoint, model id or conversation title typed within 600 ms of backgrounding the app was lost; before the debounce existed `saveSettings` ran per keystroke and there was nothing to lose. It now cancels the timer and writes through first. (2) Local's `_selectedModel` holds the Target Model ID verbatim, which is blank whenever the user means Auto, so the reply bubble was labelled with an empty string and every session it saved recorded one; it was `Local Network AI` before `0.7.30.4`. New `ChatProvider.activeModelId` resolves Local through `effectiveLocalModel` and replaces four hand-rolled copies of that same ternary. (3) `SystemPromptPanel._syncControllers` keyed its guard on focus, so a config pack import or character card load arriving while the textarea held focus was dropped, leaving a stale draft on screen that the next keystroke wrote back over the prompt that had just loaded. The guard now compares against the value the panel last saw, which the user's own echo matches and an external write does not, so typing is still uninterrupted and an import still lands. Guarded by `test/regression_0_7_30_5_test.dart`, whose flush and import cases fail against `0.7.30.5`. | done |
| 0.7.30.7 | N | **An authentication failure now says what AIRP sent.** A 401 body reports what the server could not find, never what arrived, and two opposite failures read identically from the outside: a key the server rejected, and no key at all. `ChatApiService.describeCredential` appends the shape of the credential used to every 401 and 403 on the OpenAI-compatible paths: no key, whitespace only, or a length plus whether it carried surrounding or embedded whitespace. Never the key itself, which `test/credential_diagnostic_test.dart` asserts. Verified against the live OpenRouter endpoint: `Missing Authentication header` is returned for a bare `Bearer`, an empty token, no scheme at all, and for any separator other than one ASCII space, including a tab, a double space or a non-breaking space; a real but invalid key returns `User not found.` instead. So that message means the token never arrived, which the diagnostic now distinguishes from a key the gateway refused. | done |
| 0.8.0 | — | Target release. Remaining scope in `docs/audits/`. | in progress |

## Key decisions

Preserve these across context compaction.

*   **Reasoning:** persist in session JSON for redisplay; strip the thinking tag
    ONLY from the outbound LLM payload, never from displayed or stored text. The
    per-bubble "Show Thought Process" chip stays; no new input-area control.
*   **Summarize branching:** the new conversation is [narrative summary] + [voice
    samples] + [last N assistant/user pairs]. The slider counts assistant turns and
    pairs are anchored on the assistant side. The original conversation is untouched.
*   **Config Packs:** capture ALL settings-drawer state except conversations and API
    keys. SillyTavern support is IMPORT-ONLY. The old per-rule store was discarded
    with no migration.
*   **Web search:** no Smart/Eager modes. Tell the LLM it has the tool with the
    configured limits and let it decide. Reasoning must remain visible during tool
    calls.
*   **Lore:** history scanning was removed deliberately in `0.6.11.1` and is not
    coming back without an explicit decision. Current-input recognition is the whole
    feature. See the lore section in `ARCHITECTURE.md`.
