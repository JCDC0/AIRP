/// The current application version.
///
/// This is the single source of truth for the version displayed in the UI.
/// Update this value here and in `pubspec.yaml` when releasing a new version.
const String appVersion = '0.5.6';
