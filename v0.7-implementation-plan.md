# AIRP v0.7 Execution Plan: Stabilization & Modernization

**Canonical Authority:** GEMINI.md  
**Current Version:** 0.7.1  
**Target Roadmap:** 0.7.0 → 0.7.x

> [!IMPORTANT]
> **MANDATORY AI INSTRUCTION:** Every major or minor revision/deletion completed by an AI agent **MUST** be followed by a git commit. 
> Do not batch multiple roadmap slices into a single commit. 
> Follow the versioning and commit format rules strictly.

---

## Commit & Versioning Rules
- **Major Revisions/Deletions:** Bump `0.7.x` (e.g., `0.7.0`, `0.7.1`).
- **Minor Revisions:** Bump `0.7.x.x` (e.g., `0.7.1.1`).
- **Commit Format:**
  ```
  [version] - [title]
  [Summary of changes]
  ```


---

## Phase 1: Foundation Cleanup (v0.7.0)
**Objective:** Remove dead code and fix blocking analyzer warnings.

### Tasks
- [x] **Delete Orphaned/Empty Files:**
  - `lib/providers/library_provider.dart` (Empty)
  - `lib/widgets/settings_panels/library_panel.dart` (Orphaned by `settings_library_panel.dart`)
  - `lib/widgets/settings_panels/preset_panel.dart` (Orphaned by `settings_library_panel.dart`)
- [x] **Modernize Deprecated APIs:**
  - `chat_input_area.dart`: Replace `onKey` with `onKeyEvent`.
  - `chat_input_area.dart`: Replace `HardwareKeyboard.instance.isKeyPressed` logic.
- [x] **Fix Encapsulation Warnings:**
  - `chat_input_area.dart`: Make `_OrbitLine` public (`OrbitLine`).
  - `effects_overlay.dart`: Make `_RainDrop` public (`RainDrop`).
- [x] **Stale Comment Cleanup:**
  - `system_prompt_panel.dart`: Remove references to deleted files.

---

## Phase 2: System Trimming (v0.7.1)
**Objective:** Remove the image generation subsystem as requested.

### Tasks
- [x] **ChatProvider Cleanup:**
  - Remove `generateImageGemini` and `generateImage` methods.
  - Remove `_isImageGenerationEnabled` state and getters.
  - Remove image generation persistence logic.
- [x] **ChatApiService Cleanup:**
  - Remove `generateImageGemini` and `generateImage` endpoints and methods.
- [x] **UI Removal:**
  - `chat_input_area.dart`: Remove the image generation toggle button and associated logic.
  - `settings_drawer.dart`: Remove image generation settings.
- [x] **Model Cleanup:**
  - Evaluate `ChatMessage` for removal/deprecation of the `aiImage` field.
**Current Version:** 0.7.2  

---

## Phase 3: Documentation Sync (v0.7.2)
**Objective:** Align external documentation with the streamlined codebase.

### Tasks
- [x] **README.md Update:**
  - Remove all mentions of image generation.
  - Update feature list to match current capabilities.
  - Ensure setup instructions match the simplified architecture.
---

## Phase 4: Architectural Decomposition (v0.7.3+)
**Objective:** Break down "God Class" files and "Mega Widgets" into modular services and components.

### Sequential Slices
- [ ] **0.7.3: Session & Persistence Service**
  - Extract session management, auto-save, and backup logic from `ChatProvider` into `SessionService`.
- [ ] **0.7.4: Model Registry Service**
  - Extract model list management and provider-specific fetch logic into `ModelRegistryService`.
- [ ] **0.7.5: API Key & Security Service**
  - Extract API key storage and secure retrieval logic into `ApiKeyService`.
- [ ] **0.7.6: Chat Input Decomposition**
  - Break `chat_input_area.dart` into specialized widgets (InputBar, AttachmentList, SearchToggle).
- [ ] **0.7.7: Message Bubble Decomposition**
  - Break `message_bubble.dart` into render units (MarkdownBody, ActionButtons, MetadataBar).

---

## Validation Protocol
1. **Analyze:** Run `analyze_files` after every slice. Target: 0 issues.
2. **Test:** Run `run_tests`. Target: 320/320 passing.
3. **Format:** Run `dart_format` and `dart_fix` before every commit.
